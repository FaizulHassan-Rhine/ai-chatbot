export const metadata = {
  title: "Chat",
  description:
    "Chat with Hermes AI — streaming responses, optional live web context, and model selection.",
};

export default function ChatLayout({ children }) {
  return children;
}
